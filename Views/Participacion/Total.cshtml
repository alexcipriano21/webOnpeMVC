@model webOnpeMVC.Models.ParticipacionTotal
@{
    ViewData["Title"] = "Participación - Detalle";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.MenuActivo = "Participacion";
}

<div id="imprimelo3">
    <div class="container">
        <img src="~/content/images/f-participacion.jpg" width="1170" height="248" class="img-responsive mg30top" alt="Participación">

            @await Html.PartialAsync("_MenuNavegacion")
            <br /><br /><br /><br />

            <section class="contenedor">
                <div class="row">
                    <div class="col-xs-12 col-md-3">
                        <div class="menu-interna">
                            <ul>
                                <li><a href="@Url.Action("Index","Participacion")" class="act-izq">TOTAL</a></li>
                            </ul>
                        </div>
                    </div>

                    <div class="col-xs-12 col-md-9" id="impreso">
                        <div class="contenido-interna">
                            <div class="titulos col-xs-12">
                                <div class="col-xs-11">
                                    <h3>
                                        <span class="glyphicon glyphicon-circle-arrow-right" aria-hidden="true" style="font-size:19px"></span>
                                        SEGUNDA ELECCIÓN PRESIDENCIAL 2016: PARTICIPACIÓN CIUDADANA
                                    </h3>
                                </div>
                                <div class="col-xs-1 oculto-boton-print">
                                    <button onclick="window.print()"><i class="fa fa-print ico-print"></i></button>
                                </div>
                            </div>

                            <table>
                                <tbody>
                                    <tr>
                                        <td>
                                            <button type="button" class="btn btn-primary" onclick="history.go(-1);">
                                                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> REGRESAR
                                            </button>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>

                            <div class="col-xs-12">
                                <p class="subtitle">ACTAS CONTABILIZADAS</p>
                                @await Html.PartialAsync("_ActasContabilizadas")
                            </div>

                            @if (Model.Resultados != null && Model.Resultados.Count > 0)
                            {
                                <div class="col-xs-12 line pbot30">
                                    <div class="col-xs-12 col-md-5" style="text-align:center">
                                        <canvas id="pieChart3d" width="300" height="260" style="display:block; margin:0 auto;"></canvas>
                                        <p style="font-size:12px; margin-top:6px;">
                                            <span style="display:inline-block;width:12px;height:12px;background:#1F3864;margin-right:4px;"></span>Participación
                                            &nbsp;&nbsp;
                                            <span style="display:inline-block;width:12px;height:12px;background:#BFBFBF;margin-right:4px;margin-left:6px;"></span>Ausentismo
                                        </p>
                                    </div>

                                    <div class="col-xs-12 col-md-7">
                                        <div class="cont-recto" style="margin-bottom:10px; line-height:1.8;">
                                            Ámbito: @Model.Ambito
                                            @foreach (var detalle in Model.DetallesAmbito)
                                            {
                                                <br />
                                                @detalle
                                            }
                                        </div>
                                        <p class="subtitle">ELECTORES HÁBILES @Model.TotalEH.ToString("N0")</p>
                                        <div id="page-wrap">
                                            <table class="table09_2" cellspacing="0">
                                                <thead>
                                                    <tr>
                                                        <th>PARTICIPACIÓN</th>
                                                        <th>AUSENTISMO</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <tr>
                                                        <td>TOTAL: @Model.TotalTV.ToString("N0")</td>
                                                        <td>TOTAL: @Model.TotalTA.ToString("N0")</td>
                                                    </tr>
                                                    <tr>
                                                        <td>% TOTAL: @(Model.PctTVStr)%</td>
                                                        <td>% TOTAL: @(Model.PctTAStr)%</td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>

                                <script src="~/js/pieChart3d.js"></script>
                                <script>
                                                            document.addEventListener("DOMContentLoaded", function() {
                                                                drawPieChart3d('pieChart3d', @Model.TotalTV, @Model.TotalTA, '@Model.PctTVStr%', '@Model.PctTAStr%');
                                                            });
                                </script>
                            }

                            @if (Model.MostrarTabla && Model.Resultados != null)
                            {
                                <div class="col-xs-12">
                                    <p class="subtitle">CONSULTA DE PARTICIPACIÓN DETALLADO</p>
                                    <div id="page-wrap" style="margin-bottom: 40px;">
                                        <table class="table21">
                                            <tbody id="resultados">
                                                <tr class="titulo_tabla">
                                                    <td>@Model.ColHeader</td>
                                                    <td>TOTAL ASISTENTES</td>
                                                    <td>% TOTAL ASISTENTES</td>
                                                    <td>TOTAL AUSENTES</td>
                                                    <td>% TOTAL AUSENTES</td>
                                                    <td>ELECTORES HÁBILES</td>
                                                </tr>
                                                @foreach (var r in Model.Resultados)
                                                {
                                                    <tr style='cursor:pointer;' onmouseover="this.style.color='grey'" onmouseout="this.style.color='black'" onclick="window.location='@r.Url'">
                                                        <td>@r.Nombre</td>
                                                        <td>@r.TotalVotantes.ToString("N0")</td>
                                                        <td>@r.PctTotalVotantes</td>
                                                        <td>@r.TotalAusentes.ToString("N0")</td>
                                                        <td>@r.PctTotalAusentes</td>
                                                        <td>@r.ElectoresHabiles.ToString("N0")</td>
                                                    </tr>
                                                }
                                                <tr>
                                                    <td><strong>TOTALES</strong></td>
                                                    <td><strong>@Model.TotalTV.ToString("N0")</strong></td>
                                                    <td><strong>@Model.PctTVStr %</strong></td>
                                                    <td><strong>@Model.TotalTA.ToString("N0")</strong></td>
                                                    <td><strong>@Model.PctTAStr %</strong></td>
                                                    <td><strong>@Model.TotalEH.ToString("N0")</strong></td>
                                                </tr>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            }

                        </div>
                    </div>
                </div>
            </section>
        </div>
    </div>
